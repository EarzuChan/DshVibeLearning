# Local dev overlay: load this plugin from TypeScript source during the
# `pnpm dsh web --patch ./cordis.yml` loop. The row references the source
# path relative to this directory (the DSH CLI's tsx source launch).
- insert:
    - id: dsh-vibe-learning
      name: './src/index.ts'
